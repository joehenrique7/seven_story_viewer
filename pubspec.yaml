name: seven_story_viewer
description: "Atleta do Vôlei story viewer widget — supports image, video and text stories."
version: 0.1.2
homepage:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.9.2
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
